const utilities = {

  cleanText(response) {

    const cleanedResponse = response.results.map(question => {
        return true

      //take the question and the correct answer and the incorrect answers and substittute the quptes and apostrophes for the real deal. consider using a regular expression
    })
    return cleanedResponse
  }

};

export default utilities;
